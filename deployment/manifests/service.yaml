apiVersion: v1
kind: Service
metadata:
  name: credit-scoring-service
spec:
  selector:
    app: credit-scoring-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP